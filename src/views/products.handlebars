<div>
    <h1><b>Tienda de botellas</b></h1>
    <h2><b>Productos</b></h2>
    
    <h3>Pg {{products.page}} / {{products.totalPages}} (sig -> {{products.nextPage}})</h3>

    <form action="/views/products/1">
    <button>Pag 1</button>
    </form>
    <form action="/views/products/2">
    <button>Pag 2</button>
    </form>
    <form action="/views/products/3">
    <button>Pag 3</button>
    <ul class="list-group">
        {{#each products.docs}}
            <li class="list-group-item">{{this.title}} -> {{this.description}} ({{this.size}}): {{this.price}}</li>
        {{/each}}
    </ul>
</div>